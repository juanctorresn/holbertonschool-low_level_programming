#include <stdio.h>
/**
 * main - using printf
 * Use the function printf, compile without warning
 * when using the -Wall gcc option
 * Return: value 0
 */
int main(void)
{
  printf("with proper grammar, but the outcome is a piece of art,\n");
  return (0);
}
